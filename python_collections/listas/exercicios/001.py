# from array import array

numeros = []
# numeros_1 = array('B', [])

for i in range(0, 5):
    numeros.append(input(f"Digite o valor da Posicao {i}: "))
    # numeros_1.insert(i, int(input(f"Digite o valor da Posicao {i}: ")))

print(numeros)
# print(numeros_1)
